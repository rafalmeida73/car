import { DefaultTheme } from "react-native-paper";

export const reactNativePaperTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#FF5C00",
    background: "#fff",
  },
};
